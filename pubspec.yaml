name: flutter_event_bus_chat
description: Flutter Event Bus chat/message example
version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  event_bus: ^2.0.1
  yaml: ^3.1.3

flutter:
  assets:
    - assets/config.yaml
