name: github_pagination_demo
description: A demo for paginated GitHub issues in Flutter.
publish_to: 'none'
version: 0.0.1
homepage: https://github.com/manjeetsinghgoyal/flutter_sample/tree/pagination

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0

flutter:
  uses-material-design: true
  assets:
    - assets/issues.json