name: github_pagination_demo
description: A demo for paginated GitHub issues in Flutter.
publish_to: 'none'

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0

flutter:
  uses-material-design: true
  assets:
    - assets/issues.json